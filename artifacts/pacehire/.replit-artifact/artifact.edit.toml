kind = "web"
previewPath = "/"
title = "PaceHire"
version = "1.0.0"

[[services]]
localPort = 3000
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/pacehire run dev"

[services.development.env]
PORT = "3000"

[services.production]

[services.production.build]
args = ["pnpm", "--filter", "@workspace/pacehire", "run", "build"]

[services.production.build.env]
NODE_ENV = "production"

[services.production.run]
args = ["pnpm", "--filter", "@workspace/pacehire", "run", "start"]

[services.production.run.env]
PORT = "3000"
NODE_ENV = "production"
